<?php
function ds_1col_nowrap() {
  return array(
    'label' => t('One column no wrap'),
    'regions' => array(
      'middle' => t('Content'),
    ),
    // Uncomment if you want to include a CSS file for this layout (example_layout.css)
    // 'css' => TRUE,
    // Uncomment if this is a template for a node form.
    // 'form' => TRUE,
  );
}
